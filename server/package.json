{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "node -e \"console.log('Run setup.bat on Windows or setup.sh on Unix-based systems')\"",
    "db:create": "echo \"Create database: CREATE DATABASE transparatech_db;\"",
    "db:schema": "psql -d %DB_NAME% -f src/database/schema.sql",
    "db:reset": "echo \"This will reset your database. Make sure to backup first!\"",
    "lint": "echo \"Add ESLint configuration for code linting\"",
    "format": "echo \"Add Prettier configuration for code formatting\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "joi": "^17.13.1",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
